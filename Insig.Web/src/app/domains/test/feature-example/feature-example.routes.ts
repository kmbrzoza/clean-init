import { Routes } from '@angular/router';

import { ExampleComponent } from './example.component';

export const FEATURE_EXAMPLE: Routes = [
    {
        path: '',
        component: ExampleComponent
    }
]
