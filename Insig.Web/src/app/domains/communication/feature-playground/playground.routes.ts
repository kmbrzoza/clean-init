import { Routes } from '@angular/router';

import { PlaygroundComponent } from './playground.component';

export const FEATURE_PLAYGROUND_ROUTES: Routes = [
    {
        path: '',
        component: PlaygroundComponent
    }
]
